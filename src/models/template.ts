export type Template = {
  id: string;
  name: string;
  description: string;
  created: Date;
  backgroundImage: string;
  orientation: number;
  grid: number;
  gridColor: string;
  showGrid: boolean;
}
